# Database
DATABASE_URL=sqlite+aiosqlite:///./avisio.db

# JWT
JWT_SECRET=change-me-to-a-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=72

# CORS
# CORS_ORIGINS=["http://localhost:5200"]

# AI (mock for now)
AI_API_KEY=your_api_key_here
AI_API_BASE=https://api.openai.com/v1
AI_MODEL=gpt-4o

# Rate limit
RATE_LIMIT_PER_MIN=60
